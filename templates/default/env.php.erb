<?php
/*
 * Environment Variables for application <%= @application %>
 */

# ENV Variables
<% @env_vars.each_pair do |k, v| %> 
  putenv('<%= "#{k}=#{v}" %>');
<% end %>

# INI Sets
<% @ini_sets.each_pair do |k, v| %> 
  ini_set('<%= k %>', '<%= v %>');
<% end %>